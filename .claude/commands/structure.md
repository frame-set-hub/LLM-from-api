Show the current project directory tree (excluding .git, vendor, binaries) and provide a brief summary of each package's responsibility and how they connect.

Keep the output concise — focus on architecture, not individual functions.
