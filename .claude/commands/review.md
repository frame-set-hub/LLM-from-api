Review the current uncommitted changes (staged + unstaged) in this repository.

Focus on:
1. **Correctness** — bugs, logic errors, nil pointer risks
2. **Security** — credential leaks, injection, unsafe input handling
3. **Go idioms** — error wrapping, naming conventions, package structure
4. **Performance** — unnecessary allocations, missing buffer reuse, unbounded growth

Run `git diff` and `git diff --cached` to see all changes, then provide a concise review with actionable suggestions.
